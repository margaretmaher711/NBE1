import {StyleSheet} from 'react-native';

const getStyles = () =>
  StyleSheet.create({
    homeShowCards: {
      flexDirection: 'row',
      justifyContent: 'space-around',
    },
  });

export default getStyles;
